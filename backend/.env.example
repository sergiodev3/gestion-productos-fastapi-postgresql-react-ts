# ============================================================================
# VARIABLES DE ENTORNO - Backend
# ============================================================================
# Copia este archivo como .env y ajusta los valores según tu configuración

# ============================================================================
# BASE DE DATOS
# ============================================================================
# PostgreSQL connection string
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_base_datos
# 
# Desarrollo local:
DATABASE_URL=postgresql://postgres:password@localhost:5432/gestion-productos

DB_HOST=localhost
DB_PORT=5432
DB_NAME=gestion-productos
DB_USER=postgres
DB_PASSWORD=your_password_here
#
# Producción (Railway):
# DATABASE_URL se configura automáticamente en Railway
# No necesitas especificarla aquí para producción

# ============================================================================
# CORS - Orígenes permitidos
# ============================================================================
# Desarrollo local (permitir todos los orígenes):
ALLOWED_ORIGINS=*
#
# Producción (especificar dominios permitidos):
# ALLOWED_ORIGINS=https://tu-frontend.vercel.app,https://www.tu-dominio.com
#
# Múltiples orígenes separados por comas:
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,https://tu-app.vercel.app

# ============================================================================
# SERVIDOR
# ============================================================================
# Host del servidor (0.0.0.0 permite conexiones desde cualquier IP)
HOST=0.0.0.0

# Puerto del servidor
PORT=8000

# Auto-reload en desarrollo (true) o producción (false)
RELOAD=true

# ============================================================================
# NOTAS IMPORTANTES
# ============================================================================
# 1. NO SUBIR ESTE ARCHIVO A GIT CON DATOS REALES
# 2. Agregar .env al archivo .gitignore
# 3. En Railway, configurar las variables en el dashboard
# 4. El archivo .env solo se usa en desarrollo local
