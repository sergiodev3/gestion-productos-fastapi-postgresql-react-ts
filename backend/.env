# Archivo .env para configuración del backend (desarrollo)
# Variables disponibles:
# HOST: Dirección donde se servirá la API (por defecto 0.0.0.0)
# PORT: Puerto donde se servirá la API (por defecto 8000)
# RELOAD: Si se debe habilitar reload automático (true/false)
# ALLOWED_ORIGINS: Orígenes permitidos por CORS. Use '*' o una lista separada por comas.

HOST=127.0.0.1
PORT=8000
RELOAD=true
# Ejemplo para permitir el Live Server de VSCode.
# Live Server puede usar puerto 5500 o 5501 según la configuración del VS Code.
# Puedes poner '*' para permitir todos los orígenes en desarrollo.
ALLOWED_ORIGINS=http://127.0.0.1:5500,http://127.0.0.1:5501
